# MKR iOS Messenger

MKR - это защищённый мессенджер с открытым исходным кодом, основанный на протоколе Delta Chat. Использует end-to-end шифрование и работает на любом IMAP/SMTP сервере.

## Особенности

- 🔐 **End-to-end шифрование** - AutoCrypt и OpenPGP
- 📱 **Голосовые и видеозвонки** - WebRTC
- 🌐 **Децентрализованный** - работает на любом почтовом сервере
- 👥 **Групповые чаты** - без ограничений на количество участников
- 🎨 **Кастомизация под бренд MKR** - синяя цветовая схема
- 📎 **Отправка файлов** - изображения, видео, документы
- 🎤 **Голосовые сообщения**
- 📍 **Отправка локации**
- ✅ **Стикеры и реакции**
- 🔍 **QR-код для добавления контактов**

> ✅ **Enterprise сертификат**: Приложение работает **1 год**, устанавливается на **любые устройства** без ограничений!

## Структура проекта

```
deltachat-ios-main/
├── deltachat-ios/              # Основное приложение
│   ├── MKRConfig.swift         # Конфигурация MKR серверов
│   ├── Assets.xcassets/        # Изображения, цвета, иконки
│   └── ...                     # UI контроллеры, вьюхи
├── DcCore/                     # Core библиотека (Swift + Rust)
├── DcWidget/                   # Home Screen Widget
├── DcShare/                    # Share Extension
├── DcNotificationService/      # Notification Service Extension
├── .github/workflows/          # GitHub Actions CI/CD
│   └── build-mkr.yml          # Workflow для сборки MKR
└── MKR_SERVER_SETUP.md         # Инструкция по настройке серверов
```

## Требования

- **macOS**: 15.0+ (Sequoia) - для сборки
- **Xcode**: 16.2+ - для сборки
- **iOS Target**: 13.0+
- **Swift**: 5.9+
- **Rust**: (устанавливается через cargo)

**Для пользователей**: Только iPhone/iPad с iOS 13+

## Быстрый старт

### Для пользователей: Установка приложения

1. **Скачайте `.ipa` файл**
   - Из релизов на GitHub
   - Или из GitHub Actions Artifacts

2. **Установите на iPhone** (3 способа):

   **Способ 1: Прямая установка (рекомендуется)**
   - Отправьте `.ipa` на устройство (Telegram, Email)
   - Откройте файл и нажмите "Установить"
   - Подтвердите в Настройки → Основные → Профили

   **Способ 2: Через Sideloadly**
   - Скачайте [Sideloadly](https://sideloadly.io/)
   - Подключите iPhone к компьютеру
   - Перетащите `.ipa` в Sideloadly

   **Способ 3: Через AltStore**
   - Установите [AltStore](https://altstore.io/) на iPhone
   - Откройте `.ipa` через Safari

### Для разработчиков: Сборка через GitHub Actions

1. **Добавьте Variables в GitHub**
   - `ESIGN_CERT_P12` - сертификат в Base64
   - `ESIGN_CERT_PASSWORD` - пароль

2. **Push в GitHub**
   ```bash
   git add .
   git commit -m "Update MKR"
   git push
   ```

3. **GitHub Actions автоматически соберёт `.ipa`**
   - Перейдите в **Actions** → **Build MKR**
   - Скачайте артефакт `MKR-iOS-Enterprise`

## Настройка серверов

MKR использует собственную серверную инфраструктуру. Подробная инструкция в [MKR_SERVER_SETUP.md](MKR_SERVER_SETUP.md).

### Кратко:

1. **IMAP/SMTP** - для обмена сообщениями
   - IMAP: `imap.mkr.su:993` (SSL)
   - SMTP: `smtp.mkr.su:465` (SSL)

2. **WebRTC** - для звонков
   - Signaling: `wss://signaling.mkr.su`
   - TURN: `turn:mkr.su:3478`
   - STUN: `stun:mkr.su:3478`

3. **Media Storage** - для файлов (опционально)
   - API: `https://media.mkr.su`

Автоматическая настройка для email `@mkr.su` уже реализована в [MKRConfig.swift](deltachat-ios/MKRConfig.swift).

## GitHub Actions CI/CD

Автоматическая сборка настроена в [.github/workflows/build-mkr.yml](.github/workflows/build-mkr.yml).

### Jobs:

1. **build-mkr-simulator** - автоматическая при push/PR
   - Собирает для iOS Simulator
   - Запускает тесты
   - Не требует сертификата

2. **build-mkr-enterprise** - автоматическая при наличии сертификата
   - Собирает для реальных устройств
   - Подписывает Enterprise сертификатом
   - Создаёт `.ipa` файл
   - Сохраняет артефакт на 90 дней

### Настройка Variables

**Settings** → **Secrets and variables** → **Actions** → **Variables**:

| Variable | Описание | Обязательно |
|----------|----------|-------------|
| `ESIGN_CERT_P12` | Сертификат .p12 в Base64 | Да |
| `ESIGN_CERT_PASSWORD` | Пароль сертификата | Да |

Подробная инструкция: [.github/BUILD_SETUP.md](.github/BUILD_SETUP.md)

## Кастомизация бренда

### Цвета

Основные цвета определены в [DcColors.swift](DcCore/DcCore/Helper/DcColors.swift):

```swift
// Основной синий цвет MKR
public static let primary = UIColor(hexString: "0066CC")

// Цвет сообщений
public static let messagePrimaryColor = UIColor.themeColor(
    light: UIColor.rgb(red: 200, green: 220, blue: 255),
    dark: UIColor.init(hexString: "0A2A5C")
)

// Цвет бейджей
public static let unreadBadge = UIColor(hexString: "0066CC")
```

### Локализация

Название приложения обновлено во всех языках в `*.lproj/Localizable.strings`:

```
"app_name" = "MKR";
```

### Bundle ID и URL Schemes

- **Bundle ID**: `com.mkr.su`
- **App Group**: `group.com.mkr.su`
- **URL Schemes**: `mkr://`, `mkraccount://`, `mkrlogin://`, `mailto:`

## Архитектура

### Основные компоненты

- **DcCore** - Core библиотека, обёртка над Rust C FFI
- **CallManager** - Управление голосовыми/видео звонками через CallKit
- **Coordinators** - Навигация (AppCoordinator, ChatCoordinator)
- **MVVM** - Pattern для UI (ViewModels + Views)

### Extension Targets

1. **DcWidget** - Home Screen Widget для быстрого доступа к чатам
2. **DcShare** - Share Extension для отправки контента из других приложений
3. **DcNotificationService** - Rich Notifications (требует платный аккаунт для Push)

## Безопасность

- ✅ AutoCrypt для автоматического управления ключами шифрования
- ✅ OpenPGP совместимость
- ✅ Face ID / Touch ID для локальной защиты
- ✅ Проверка сертификатов TLS
- ✅ SRP (Secure Remote Password) для verified пользователей

## Зависимости

Основные зависимости управляются через CocoaPods:

```ruby
# Podfile
pod 'DcCore', :path => 'DcCore'
pod 'SwiftSVG'
pod 'SwipeCellKit'
# ... и другие
```

## Enterprise сертификат

| Функция | Доступно |
|---------|----------|
| Установка на любые устройства | ✅ |
| Срок действия | ✅ 1 год |
| Количество устройств | ✅ Безлимит |
| Прямая установка | ✅ |
| Публичная дистрибуция | ✅ |
| App Store | ❌ |
| TestFlight | ❌ |
| Push уведомления | ⚠️ Требует настройки |
| In-App Purchases | ❌ |

### Возобновление сертификата

Enterprise сертификат действует **1 год**. После истечения:
1. Получите новый сертификат от Esign
2. Обновите `ESIGN_CERT_P12` в GitHub Variables
3. Пересоберите приложение
4. Пользователям нужно переустановить новую версию

## Участие в разработке

Мы приветствуем вклад в разработку!

1. Fork проекта
2. Создайте feature branch: `git checkout -b feature/amazing-feature`
3. Commit: `git commit -m 'Add amazing feature'`
4. Push: `git push origin feature/amazing-feature`
5. Pull Request

## Лицензия

MKR основан на Delta Chat, который распространяется под GPL-3.0-or-later.

## Контакты

- **GitHub**: https://github.com/YOUR_USERNAME/mkr-ios
- **Support**: support@mkr.su
- **Website**: https://mkr.su

## Благодарности

- [Delta Chat](https://delta.chat) - оригинальный проект
- Всем контрибьюторам Delta Chat iOS

---

**Made with ❤️ for MKR**
