# MKR Flutter

Кроссплатформенная версия защищённого мессенджера MKR для iOS и Android.

## Особенности

- 🔐 End-to-End шифрование (Signal Protocol)
- 🚨 Panic Button - экстренное удаление данных
- 🎭 Stealth Mode - маскировка под калькулятор
- 📱 Нативный iOS интерфейс (Cupertino)
- ☁️ Сборка iOS без Mac через Codemagic

## Структура проекта

```
mkr_flutter/
├── lib/
│   ├── core/           # Константы, темы, валидаторы
│   ├── data/           # Репозитории, сервисы, источники данных
│   ├── domain/         # Сущности, интерфейсы
│   └── presentation/   # Экраны, виджеты, провайдеры
├── ios/                # iOS-специфичный код
├── android/            # Android-специфичный код
├── test/               # Тесты
├── docs/               # Документация
└── codemagic.yaml      # CI/CD конфигурация
```

## Начало работы

### Требования

- Flutter SDK 3.24.0+
- Dart SDK 3.9.0+
- Xcode 15+ (для локальной iOS разработки на Mac)
- Android Studio (для Android разработки)

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd mkr_flutter

# Установить зависимости
flutter pub get

# Запустить кодогенерацию (Riverpod)
flutter pub run build_runner build --delete-conflicting-outputs

# Запустить на устройстве
flutter run
```

### Запуск тестов

```bash
# Все тесты
flutter test

# С покрытием
flutter test --coverage
```

## Сборка iOS без Mac

Проект настроен для сборки iOS приложений через облачный сервис Codemagic.

### Быстрый старт

1. Создайте аккаунт на [Codemagic](https://codemagic.io/)
2. Подключите репозиторий
3. Загрузите сертификаты и профили
4. Запустите сборку

### Подробная документация

См. [docs/IOS_BUILD_SETUP.md](docs/IOS_BUILD_SETUP.md) для полной инструкции по:
- Настройке Apple Developer Account
- Созданию сертификатов и профилей
- Конфигурации Codemagic
- Публикации в TestFlight

## CI/CD Workflows

### ios-release
- Триггер: push в `main` или `release/*`
- Результат: IPA для App Store, автозагрузка в TestFlight

### ios-adhoc
- Триггер: push в `develop` или `feature/*`
- Результат: IPA для прямой установки

### ios-debug
- Триггер: Pull Request
- Результат: Проверка сборки и тестов

## Архитектура

Проект использует Clean Architecture:

- **Presentation** - UI слой (Cupertino/Material виджеты, Riverpod)
- **Domain** - Бизнес-логика (Use Cases, Entities)
- **Data** - Данные (Repositories, DataSources, Services)

## Безопасность

- Все данные шифруются локально
- Ключи хранятся в Keychain (iOS) / Keystore (Android)
- Поддержка Face ID / Touch ID
- Автоудаление сообщений
- Защита от jailbreak/root

## Связанные проекты

- `app/` - Kotlin Android версия (оригинал)
- `backend/` - Ktor backend (общий для обеих версий)

## Лицензия

Proprietary - All rights reserved
